<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.interaxon.test.libmuse.MainActivity" >

    <item android:id="@+id/action_refresh"
          android:icon="@drawable/ic_action_refresh"
          android:showAsAction="ifRoom"
          android:title="Refresh"/>

    <item android:id="@+id/action_drowsy_data"
          android:showAsAction="never"
          android:title="Use drowsy data"/>

    <item android:id="@+id/action_connect"
          android:showAsAction="never"
          android:title="Connect"/>

    <item android:id="@+id/action_disconnect"
          android:showAsAction="never"
          android:title="Disconnect"/>

</menu>
